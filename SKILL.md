---
name: prd-generator
description: 依据原型与说明，用标准 PRD 章节结构（概述、全局说明、产品需求详述、埋点与数据、风险与相关文档等）和类型化模板撰写可评审的 Markdown；默认先产出「产品需求详述」级内容，需要时再补全其余章节。支持整页、局部迭代与 AI/策略类需求；信息不足时不杜撰正文、先追问。适用于撰写/更新 PRD、按原型写需求、埋点与数据需求、仅详述或全文档输出。（勿向读者暴露「第N章」「骨架」等内部编排用语，见正文「读者可见正文 vs 内部编排」。）
---

<!-- Miller Zhang -->
# 从原型到 PRD（prd-generator）

## 读者可见正文 vs 内部编排（必遵）

撰写**将转发给研发、测试、业务的 Markdown PRD**时，与只在对话里解释 Skill 自身时，用语必须区分清楚：

- **禁止写入 PRD 正文**（含文前导语、章节引言、括号里对文档结构的自我说明）：`第3章` / `第九章` / `9章` / `PRD骨架` / `骨架文件` / `本 Skill` / `模板`+文件名（如 `list-page.md`）/ `full-prd-skeleton` 等**编排或工具元信息**。读者未读过本 Skill 时，这些词会像「黑话」且易被理解成业务内容。
- **允许且推荐**：直接使用标准目录中的**正式标题与编号**，例如 `## 1 概述`、`## 3 产品需求详述`、`### 3.2 xxx`、`## 4 埋点统计`、`### 2.4 全局交互与展示约定`。标题本身已表达结构，**不要再**写「以下为第 3 章」「按九章组织」之类叠床架屋的句子。
- **交叉引用**（正文内）：写「与本文 **2.4 全局交互与展示约定** 一致」「见 **3 产品需求详述** 中【某功能名】」，避免「同第 2 章」「见第 3 章某节」。
- **澄清与反问**（对话里）：用**章节标题**描述缺什么，例如「还需要「概述」里的背景与范围」「要写「埋点统计」请补充关键操作清单」；避免「缺第 1 章材料」——除非用户自己一直用「第 N 章」说法，可与之对齐。
- **使用 `template/*.md` 时**：文件顶部的 `>` 提示块主要给智能体看；**填入用户业务内容后的交付稿中不要原样保留**整块编排说明；若必须留一句衔接，改为业务语言（例如「本列表默认排序与本文 2.4 一致」）。

后文为便于智能体对齐习惯，仍可能出现「顶级章 3」「1～9 章」等**对内简称**；**凡写入用户可见 PRD，一律改为上表中的正式章节名与 Markdown 标题**，勿把简称当正文写给读者。

## 任务目标

- 本 Skill 用于：根据用户提供的需求说明、附件与**原型图（或静态页说明）**，生成结构化的 **Markdown 格式产品需求文档（PRD）**。
- 能力包含：
  - **原型解读（内部分析）**：识别界面结构、筛选/列表/表单/图表等区域，以及红框/批注所指的增删改点；结论写入 **「3 产品需求详述」** 对应小节正文，**不单独成章输出「原型分析报告」**，除非用户明确要求附录式「对照说明」。
  - **单功能产品需求详述**：为每个页面或局部功能，选择匹配的类型模板，输出可直接评审的 Markdown；**页面类**在 **「1 原型截图」** 用 **`![](相对路径)`** 嵌入原型/设计稿，便于评审对照（见 **注意事项 · 原型图嵌入**）。
  - **AI / 策略类需求详述**：无完整页面时，使用策略模板，强调输入输出、效果预期与 Bad Case。
  - **整份 PRD 组装**：在全部**产品需求详述**小节就绪后，按 `references/full-prd-skeleton.md` 的**标准目录**补全概述、全局说明、埋点、数据、风险等，并统一术语与编号。
  - **交付前三角色自查与修订（每轮自动，无需用户触发）**：凡本轮**已产出**可交付的 PRD Markdown（仅 **3** 或**完整 PRD**），智能体须在**同一轮对话内、在把正文发给用户之前**，**自动**打开并执行 [`references/triple-role-self-review.md`](references/triple-role-self-review.md)，**禁止**等待用户再说「请自查」「复核一下」「定稿前审查」等才执行。从**产品、开发、测试**三视角审视后**直接修订正文**（不把自查表或「角色扮演过程」写入 PRD），缺口仍用 **待确认** 与 **「8 待完善」**。**唯一例外**：用户在本轮**明确说**「跳过三角色自查」「不要自查」时，可省略第 7 步。
- **触发关键词示例**：「创建 PRD」「撰写 PRD」「更新 PRD」「产品需求文档」「需求详述」「按原型写需求」「产品需求详述」「埋点」「数据需求」等。（用户若说「第 3 章」指顶级「3 产品需求详述」，智能体理解即可，**输出 PRD 时仍以正式标题呈现**。）

### 与 `create-prd` 技能的选用

- **`create-prd`（8 节英文向）**：偏商业/干系人可读——Summary、Contacts、Background、Objective、Market、Value、Solution、Release 等，适合要一份「从目标到发版」的通稿型 PRD、或强调整体叙事与发布节奏时。
- **本技能 `prd-generator`（9 章中文 + 详述模板）**：偏**研发/测试/评审**——以「3 产品需求详述」为核心，配套埋点/数据/风险/待完善等标准章，**强依赖原型或等价界面材料**、类型化 `template/*.md` 与 `full-prd-skeleton.md` 目录。
- 若用户未指定：默认按**本技能**；若其描述明显贴近 `create-prd` 的八段结构，可**反问一句**以本次用哪套结构为准，避免混写两套标题体系。

### 智能体速查（每轮必过）

1. **意图**：无「完整 / 全章 / 1～9 / 含概述」等**显式**信号 → 默认**仅**输出 `## 3 产品需求详述` 及下挂 `### 3.x …`；有全章明确信号再按 `full-prd-skeleton.md` 汇编 **1～9**。
2. **门槛**：未达**最低可写条件** → **不**生成 PRD 正文，只作说明 + 合并追问（见**意图澄清与对话策略**）。
3. **详述**：在「模板选型」中选主模板 `template/*.md`；**页面类**须在「1 原型截图」写 `![](相对路径)`（见**注意事项 · 原型图嵌入**）。
4. **全章**（若需要）：补 **1、2、4～9**；**2.4** 默认条文见 `global-ui-conventions.md`；Mermaid 见**注意事项 · 图表（Mermaid）**：`flowchart` 中 `+` `/` 等**须双引号**包文案；**勿**用 `&` 多路连写；`mindmap` 慎用，**优先** `flowchart`。
5. **定稿**：在把正文交给用户**之前**，**自动**执行 `triple-role-self-review.md` 并**合并修订**到同一稿（用户未说「跳过三角色自查」时不得省略）。

## 前置准备

- 具备 **图像理解能力**（识别截图中的控件、文案、状态、批注与红框）。
- **无需安装额外依赖**；输出为 Markdown，图表优先使用 **Mermaid**。

---

## PRD 标准章节（必须与 `full-prd-skeleton.md` 一致）

撰写或拼接完整 PRD 时，采用 **9 个顶级章节**（对内可称「九章结构」，**勿在 PRD 正文里写「九章」「骨架」等字样**）；其中「1 概述」「2 全局说明」下含小节如下（与 `full-prd-skeleton.md` 标题一致）：

| 顶级章 | 章节名称 | 内含小节 |
| --- | --- | --- |
| 1 | 概述 | 1.1 背景说明；1.2 需求范围；1.3 用户描述；1.4 阅读对象；1.5 名词解释 |
| 2 | 全局说明 | 2.1 产品功能架构图；2.2 系统/平台角色；2.3 产品业务流程图/泳道图；**2.4 全局交互与展示约定** |
| 3 | 产品需求详述 | （无固定子编号，由模板小节拼接；**功能级**数据口径/来源/刷新/权限等宜写在本章） |
| 4 | 埋点统计 | |
| 5 | 数据需求 | （见 `full-prd-skeleton.md`：**以历史/迁移/冷启动预置**等跨功能问题为主，单功能级条款优先落 **3**） |
| 6 | 风险分析 | |
| 7 | 补充说明 | |
| 8 | 待完善 | |
| 9 | 相关文档 | |

**章节编号约定**：用户说「章节 3」指顶级「3 产品需求详述」；说「1.2」「2.3」指对应小节。智能体输出须与上表及 `full-prd-skeleton.md` 一致；**写入 PRD 时只保留标准标题与编号，不解释「章」从哪来**。

---

## 推荐工作流程（与用户团队一致）

### 阶段 A：原型准备

- 先完成主要功能页面的原型（可多页、多角色）。

### 阶段 B：逐条撰写「3 产品需求详述」（核心）

对用户**每次上传的页面截图（或局部迭代标注图）**：

1. 识别本需求属于下列哪一种（可组合）：
   - **完整页面功能**：整页为新功能或大改版。
   - **页面局部功能**：在已上线页面上做增删改；需明确**影响区域**、旧逻辑兼容与回归范围。
   - **AI 效果 / 策略类需求**：常为无界面或仅有配置界面；以**输入、输出规格、效果预期、Bad Case** 为主。
2. 在下方「模板选型」中选 **一个主模板**（必要时主模板 + 局部补充小节）。
3. 严格按模板章节输出 Markdown；**原型分析过程不写进独立章节**，直接体现为功能说明、字段说明、交互与异常条款；**页面类**须在模板 **「1 原型截图」** 中用 **`![](相对路径)`** 嵌入依据图（见 **注意事项 · 原型图嵌入**），不得仅保留「粘贴原型」类括号说明而无图片引用。
4. 为该功能分配稳定小节标题与编号（如 `### 3.3 集成任务-新建`），置于 `## 3 产品需求详述` 之下，便于后续汇编与评审追踪。

### 阶段 C：补全「3 产品需求详述」以外的标准章节

在「所有功能的产品需求详述」都已具备后，再撰写 **1 概述、2 全局说明、4～9 各章**（含 1.x、2.x；**2 全局说明**须含 **2.4 全局交互与展示约定**，默认条文见 `references/global-ui-conventions.md`），并回头检查与详述小节的术语、范围、流程是否一致；**交付前**须**自动**按 [`references/triple-role-self-review.md`](references/triple-role-self-review.md) 执行三角色自查与定稿修订并合并进同一稿，**无需**用户另行触发。

**结构目录文件**：[`references/full-prd-skeleton.md`](references/full-prd-skeleton.md)  
**章节写作要点**：[`references/prd-chapters-guide.md`](references/prd-chapters-guide.md)

---

## 模板选型（用于「3 产品需求详述」各小节）

以下模板位于 `template/` 目录，智能体应根据页面形态**择一为主**（不可混用时取最主要形态）：

| 场景 | 模板文件 |
| --- | --- |
| 列表 + 筛选 + 新建 + 批量；行内查看/编辑/删除 | [`template/list-page.md`](template/list-page.md) |
| 卡片 + 筛选 + 新建 + 批量；卡片上操作（**小节顺序与列表页模板一致**，主展示区为卡片栅格） | [`template/card-page.md`](template/card-page.md) |
| 表单录入/编辑、提交、校验、联动 | [`template/form-page.md`](template/form-page.md) |
| 多图表、指标卡、看板 | [`template/stats-dashboard-page.md`](template/stats-dashboard-page.md) |
| 详情/只读展示，基本无提交 | [`template/info-display-page.md`](template/info-display-page.md) |
| 模型/策略生成、无完整页面 | [`template/ai-strategy-requirement.md`](template/ai-strategy-requirement.md) |

**局部迭代**：在选用模板时，于文首摘要或「功能说明」中标明为「页面局部迭代」及影响范围；**「1 原型截图」** 须用 **`![](相对路径)`** 嵌入含**圈选/批注**的图（可多张：变更前/后或与全页对照）；全文默认仅描述**变更部分及其连带影响**，但仍需保证单篇可独立阅读理解。

---

## 指令解析规则

### 默认行为

- 用户**仅上传原型/说明、未指定章节**：默认输出 **`## 3 产品需求详述` 下与本图相关的一个功能小节**（使用合适模板），不展开完整标准目录全章。
- **「生成 / 写 PRD」≠ 默认全文档**（易错点）：用户说「生成这个功能的 PRD」「帮我写 PRD」「出个 PRD」等，若**未**同时出现 **「完整」「全部章节」「1～9」「含概述」「全篇」** 等明确全量信号，**一律按上条处理**——只输出 **`## 3 产品需求详述` 及下属 `### 3.x …` 功能小节**（模板内的 1～5 节等），**不要**自动补写 **1 概述、2 全局说明、4～9**。「PRD」在口语里常被当成「整份九段文档」，但本 Skill 的**默认交付粒度是详述级**，全章汇编必须来自用户的**显式**指令。
- 用户要求 **「完整 PRD」「全部章节」**：按 `full-prd-skeleton.md` 输出 **1～9 全部顶级章**（含 1.x、2.x）；其中 **3 产品需求详述** 需用户已有或当场提供各功能内容；若**不满足**下文「最低可写条件」，**不生成**完整 PRD，先按 **意图澄清与对话策略** 反问补充；满足条件后撰写，无依据处仍用「待确认」并列入 **「8 待完善」**，不虚构细节。
- **无附件且非 AI/策略类页面需求**：**不生成**页面类 PRD 正文；按 **意图澄清与对话策略** 说明原因并反问，请用户上传原型或等价材料（或先改报为 AI/策略类并补充文字规格）。

### 按章节编号输出

用户可使用：「输出章节 1、2、3、4」「输出 1.1、1.2、3」等形式。**编号必须采用本文「PRD 标准章节」与 `full-prd-skeleton.md`**，不得与历史 15 章扁平编号混用。

### 埋点与数据

- **4 埋点统计**、**5 数据需求**：仅当用户明确要求**埋点**、**数据/埋点**、**全章含 4/5** 等时，才在对应章**写细**。
- **「4 埋点统计」**：与 **3** 中**操作**一一对应（事件名、触发时机、属性等），便于对账与分析。
- **「5 数据需求」**（与 **3 的分工**）：**3** 里应已写清各子功能的**数据口径、来源、刷新/实时、权限与脱敏**等；**5** 侧重**产品级/跨功能**的**历史数据继承与迁移、冷启动预置/种子、割接与存量边界**等（详见 [`full-prd-skeleton.md`](references/full-prd-skeleton.md) 与 [`references/prd-chapters-guide.md`](references/prd-chapters-guide.md) 中 **「3」与「5 数据需求」的分工**）。**5** 中**不要**大段重复各 `3.x` 已写过的单功能指标说明；无全产品级数据方案时，**5** 可仅保留标题、正文写「无」或极短说明。

---

## 意图澄清与对话策略

在生成 PRD 前，智能体应**快速自检**输入是否足以按本 Skill 安全落地；不足时**不生成 PRD 正文**，**先反问**让用户补充，避免凭空编造界面细节或 **「1 概述」** 中的事实性内容。

### 信息不足时的统一处置（暂停生成）

- **触发**：当前上传与文字**未达到**下文「最低可写条件」，无法写出**可对照评审**、**非空壳**的本次请求范围（单详述小节 / 完整 PRD 全章等）。
- **行为**：
  - **不输出** PRD 模板正文（不写「占位版 PRD」「全篇待确认草稿」代替可交付稿）。
  - 用**简短说明**交代为何不写（缺哪类信息、会导致什么问题）。
  - 列出 **3～7 个可一次性回答的具体问题**（可附勾选式选项，如页面类型）；**合并为一条消息**，避免零散文案式追问。
  - 可提示用户：**建议补充的附件类型**（截图、展开态、HTML、文字要点等）。
- **用户补充后**：重新自检；满足最低可写条件再进入撰写流程；撰写阶段仍遵循「不臆造」，细部无依据处标 **待确认** 并落入 **「8 待完善」**。

### 最低可写条件（未满足则不生成本次请求的 PRD）

| 本次产出范围 | 至少需具备（智能体据实判断） |
| --- | --- |
| **页面类 · 仅详述小节** | 有可还原界面的材料：**截图 / HTML（或静态说明）/ 带区域与控件说明的结构化文字**；且能选定 **模板选型** 主类型，或用户已明确类型。 |
| **AI / 策略类 · 仅详述小节** | 能陈述**要解决的业务问题**，以及 **输入、输出的大致形态与边界**（可粗，但不能全无）；选用 `ai-strategy-requirement.md`。 |
| **完整 PRD（含概述与全局说明）** | 除详述所需材料外，有足以撰写 **1.1 背景、1.2 范围、1.3 用户/角色** 的要点（口述列点即可）；否则**不生成**「完整 PRD」，先反问；或建议用户改为**仅输出「3 产品需求详述」相关小节**并补充对应材料。 |
| **局部迭代** | 能指明**变更区域**（圈选、批注或文字说明「改哪里」），否则无法写清回归范围，**不生成**、先反问。 |

**与「隐含操作」的关系**：若仅缺下拉项、点击后细节等，但**主界面结构、主路径与模板类型已可确定**，视为满足页面类最低可写条件，**可以生成** PRD，未知交互标 **待确认**；若缺到**无法确定主路径、模板类型或可验收边界**，视为不满足，**不生成**、先反问（处理方式见下文 **4. 截图中无法展示的隐含操作（下拉项、点击后行为等）**）。

**对话原则**

- **先判断再输出**：未达最低可写条件时，**只作暂停说明 + 问题列表**，不输出 PRD 正文。
- **合并提问**：同一轮尽量把相关问题合并为一条消息，避免连环追问。
- **与「不臆造」一致**：已满足最低可写条件、开始撰写后，仍无依据的信息写入 **「8 待完善」** 或文中「待确认」，不得捏造权限、接口、枚举、埋点名等。

### 1. 缺少原型或等价附件（非 AI / 策略类）

**适用**：用户希望撰写**页面类**功能详述（或默认行为会落到界面条款），但**未提供**截图、HTML/静态页、设计稿导出说明、带结构标注的文档等可还原界面的材料。

**例外**：**AI / 策略类需求**（无固定页面或仅有简单配置入口）可不依赖整页原型；此时应确认是否选用 `ai-strategy-requirement.md`，并引导用户补充**输入输出、效果预期、Bad Case** 等文字材料即可。

**动作**

- 说明：本 Skill 的页面类产出依赖原型或等价材料，才能写清布局、字段、交互与验收点。
- **未达最低可写条件**：**不生成** PRD；请用户补充 **原型截图**、关键页面 **HTML**、或**带区域说明**的线框/文档；多页可分次上传。
- **仅有文字、无图**：若文字已能**结构化还原**主要区域、控件与主路径（达到「可还原界面」），可视为满足条件并生成；否则**不生成**，反问需补充的结构化描述要点或补图。

### 2. 无法判断页面/需求类型

**适用**：附件模糊、一图多态、仅产品名或一句话需求，无法可靠对应「模板选型」表中的单一主形态。

**动作**

- **不生成** PRD，直至类型可判定或用户明确选择。
- 简要说明无法自动归类的原因（可选）。
- 请用户在下列类型中**选择主类型**（必要时「主类型 + 补充说明」）：**列表页**、**卡片页**、**表单页**、**统计看板**、**信息展示页**、**AI / 策略类**。
- 若实际为多页多功能：请用户**按页/按功能**说明，或分附件上传，以便分配 **3.x** 小节编号与模板。

### 3. 完整 PRD 但概述类信息提炼不足

**适用**：用户明确要求 **完整 PRD（标准目录全章）**或包含 **「1 概述」**（1.1～1.5）、**「2 全局说明」**等，但从已有材料中**提炼不出**或**严重不足**：背景、需求范围、用户/角色描述、名词解释、流程边界等。

**动作**

- **不生成**「完整 PRD」或含概述、全局说明的汇编稿，直至上述概述要点可写。
- 不虚构业务事实；请用户用**简短口述**（可列点）补充，建议至少覆盖：**业务背景与目标**、**目标用户或角色**、**本次需求范围（做/不做）**、**关键名词或缩写**；若涉及多系统，补充**系统边界或对接关系**。
- 若用户**坚持不补充**概述：可改为**仅输出「3 产品需求详述」下各小节**（以界面/功能材料已满足最低可写条件为前提）；若仍无法写详述，则**不生成**并说明缺口。不得输出「概述、全局说明全篇待确认」式的空壳 PRD 冒充完整稿。

### 4. 截图中无法展示的隐含操作（下拉项、点击后行为等）

**适用**：静态原型或截图**无法唯一确定**的交互与数据，例如：

- **下拉按钮 / 菜单**：未展开时看不到**各菜单项文案**或后续动作。
- **下拉选择器、级联、树选择**：看不出**选项来源**（枚举、接口、字典）、**默认值**、**是否可搜索**等。
- **普通按钮、图标按钮、行内操作**：无法从图上判断**点击后**是打开弹窗、跳转路由、触发下载、发起异步任务还是仅切换局部状态。
- 其他**需展开态、悬停态或多步操作**才能看见的配置或结果。

**动作**

- 先判断是否仍满足 **最低可写条件**（主路径与模板类型已能定）：若不满足，**不生成** PRD，**集中列出**无法推断的项并反问（见上文「与隐含操作的关系」）。
- 若已满足最低可写条件：**集中列出**从材料中无法推断的项（可按控件或区域），**邀请用户**用文字或补图说明：菜单项/选项含义、点击后的页面或反馈、关键分支（若有）。
- **用户可选择不补充**隐含细节：在**已生成**的正文中，对应条目写 **待确认**，并纳入 **「8 待完善」**，**不得编造**具体选项、跳转目标或接口行为。

### 5. 其他建议澄清场景（补充）

| 场景 | 简要动作 |
| --- | --- |
| **章节意图冲突** | 用户同时说「只写详述」与「完整 PRD / 全部章节」等，确认本次以哪一指令为准。 |
| **局部迭代但影响面不清** | 提醒在图上**圈选/批注**变更区域，或文字说明「改什么、不改什么、回归关注点」。 |
| **多页挤在一张图或未标注页名** | 询问按哪一屏先生成、是否需要**分页上传**或标注页面名称，以便 **3.x 小节标题**命名一致。 |
| **要埋点/数据但无指标与操作清单** | **4**：不生成可落地的「埋点统计」正文，先反问关键行为与事件属性；**5**：无全产品级**迁移/冷启动/对账**等可写时，不杜撰，可保留章标、正文写「无」；功能级**口径/来源/权限**仍在 **3** 中追问或待确认。 |
| **角色、权限、合规敏感** | 材料未提及时，询问是否本需求涉及**权限差异、审计、个人信息**；无则写「无则默认」类说明或待确认。 |
| **极度模糊的一句话需求** | 先澄清**要解决什么问题、成功标准、是否有旧版对照**，再选模板；避免直接套模板空填。 |

---

## 操作步骤（智能体执行）

1. **解析意图并做意图澄清**：是否指定章节；是否上传图片或等价附件；是整页还是局部；是否含 AI/策略。**若用户只说「写 PRD / 生成 PRD」而无全章关键词，默认本次产出范围为「仅 3 产品需求详述」**，勿擅自扩展为 1～9 全章。对照 **最低可写条件** 与 **信息不足时的统一处置**：未满足则**不撰写 PRD**，只输出说明与问题列表；满足后再进入撰写。参考 **意图澄清与对话策略** 全节。
2. **读取模板**：打开上表对应的 `template/*.md`，**保持其章节结构与表格列**，用用户项目内容填满。
3. **图像分析与原型落盘**：识别布局与批注；**页面类**在写入「功能说明」「字段说明」等之前，先在模板 **「1 原型截图」** 按 **注意事项 · 原型图嵌入** 写入 **`![图注](相对路径)`**（多状态多图纵向排列）；对**截图无法展示的隐含操作**（下拉项、选择器选项、按钮点击后果等）按**意图澄清** 中 **「4. 截图中无法展示的隐含操作（下拉项、点击后行为等）」** 一节的动作处理；禁止无依据臆测接口字段名，未知项写入待确认并建议落入 **「8 待完善」**。
4. **汇编完整 PRD（若用户需要）**：以 `references/full-prd-skeleton.md` 为目录，将多个详述小节按产品逻辑排序；补全其余标准章节；统一名词表（**1.5**）与架构图（**2.1**）。**汇编结果中不出现本 Skill 或「骨架」字样。**
5. **一致性检查**：范围（**1.2**）与 **3 产品需求详述** 是否一致；流程（**2.3**）是否覆盖主路径与关键异常；**「6 风险分析」**是否覆盖合规、依赖与灰度。
6. **全局约定落位**：汇编完整 PRD 时撰写 **2.4 全局交互与展示约定**（默认条文见 `references/global-ui-conventions.md` 或 `full-prd-skeleton.md` 内摘要）；撰写各详述小节时，按钮防连点、列表默认排序、区块 loading、超长文案省略与 Tooltip、**进行中类状态的自动刷新**等与 **2.4** 对齐，功能级另有说明的以该小节为准。
7. **三角色自查与定稿修订（强制自动）**：本轮一旦写出拟交付的 PRD Markdown，**必须**在本轮内、交付前**自动**执行——**打开并完整执行** [`references/triple-role-self-review.md`](references/triple-role-self-review.md)，**不得**等用户补充「请自查」「过一遍三角色」等指令；将发现的问题**改写进同一稿**（仅 **3** 或**完整 PRD** 均适用），**禁止**把自查清单全文、分角色对话或「以下为自查结果」类元叙述写入 PRD 正文。若用户只要 PRD、不要过程说明，对话里可不展开自查细节，仅交付**已修订**终稿。用户**明确**说跳过自查时，可省略本步。

---

## 资源索引

| 资源 | 路径 |
| --- | --- |
| 完整 PRD Markdown 标准目录 | [`references/full-prd-skeleton.md`](references/full-prd-skeleton.md) |
| 各章写作要点（精简） | [`references/prd-chapters-guide.md`](references/prd-chapters-guide.md) |
| 全局交互与展示约定（2.4 节正文，可内嵌或引用） | [`references/global-ui-conventions.md`](references/global-ui-conventions.md) |
| 交付前三角色自查与定稿修订（第 7 步，**每轮自动生成时自动执行**） | [`references/triple-role-self-review.md`](references/triple-role-self-review.md) |
| 单功能模板 | `template/*.md`（见上表） |
| 从 Word 抽取文本（维护用脚本） | [`scripts/extract_docx.py`](scripts/extract_docx.py)（Python 3，仅用标准库，无 pip 依赖） |

**外部参考示例（用户本机，非仓库内文件）**：若用户在对话中附上 `报告范式平台+V1.1及中行二期需求+PRD.docx`，可作为**全文结构与详述深度**的参考；智能体无法访问磁盘时，请用户粘贴片段或导出 Markdown。

---

## 注意事项

- **先详述、后概述（与常见团队节奏一致）**：背景未稳时避免过早锁死实现细节；交付稿里用业务小节标题说话，不必写「先写第几章」。
- **不臆造**：权限、接口、字段枚举、埋点事件名等无依据时标注「待确认」并列入 **「8 待完善」**；**未达最低可写条件时不生成 PRD**，见 **意图澄清与对话策略**。
- **图表（Mermaid）**：架构、流程、泳道、状态等使用 Mermaid 时，**必须**用 Markdown 代码围栏包裹，且语言标记为 **`mermaid`**，例如：
  - 第一行：\`\`\`mermaid
  - 最后一行：\`\`\`
  - **不要**把 `flowchart` / `stateDiagram-v2` 等关键字直接写在围栏外，否则预览器会当作普通文本。
  - **兼容性**：`mindmap` 仅在较新 Mermaid 中支持，**GitHub 及部分编辑器内置预览可能不渲染**；**优先**用 `flowchart TB` / `flowchart LR` 表达层级或导航结构。`stateDiagram-v2` 中节点 ID **优先使用英文**（如 `listIdle`、`querying`），中文含义写在**迁移标签**或正文「图例」中，避免中文作节点名导致旧解析器失败。
  - **本地预览**：VS Code / Cursor 默认 Markdown 预览**不一定**内置 Mermaid，需安装扩展（如「Markdown Preview Mermaid Support」等）或使用支持 Mermaid 的文档站导出 HTML。

  **为 PRD 可渲染、少踩坑的 Mermaid 写法（架构图/流程图，强烈建议全遵守）**

  1. **围栏是前提**：无 **\`\`\`mermaid** … **\`\`\`\`** 整块时，**任何**内容都不会成图。交付稿**禁止**只贴裸 `flowchart` 正文。
  2. **节点/连线上的展示文案**（`[]` / `()` / `{{}}` 等形状里的**可见文字**）：凡含可能干扰解析的字符，例如 `+`、`-`、`/`、反斜杠、未转义括号、冒号、管道符、竖线、未转义双引号等，**一律**用**双引号**包住**整段**展示文案。  
     - 推荐：`a1["Agent+快速操作"]`、`b1["底栏：提交/语音/图标"]`、`c1["处理页或工作流（待确认）"]`  
     - 易失败：`a1[Agent+快速操作]`（`+` 在部分解释器里触发语法问题）、`b1[底栏提交/语音/图标]`（未加引号时 `/` 等易被误解析）。
  3. **subgraph 标题**若含**中文+符号**、斜杠、括号，同样用**双引号**包住：`subgraph s1["顶部导航"]`；避免「裸中文 + 未转义」与解析器实现差异。
  4. **禁止依赖 `&` 多路连写**（`A & B & C --> D`）：**Cursor/VSCode 等**绑定的 Mermaid 版本**未必支持**该简写，表现为**整图不显示**或**静默失败**。**交付 PRD 时**请改为**多行单源连线**，复用**同一**目标节点 `id`：  
     - 推荐：`A --> d`、`B --> d`、`C --> d`（`d` 为已声明节点）
     - 同规则适用于 `P & S & A --> x` 这类：拆成三行 `P --> x`，`S --> x`，`A --> x`。
  5. **节点 `id`（标识符）**：**仅用** `字母`、`数字`、**下划线**（如 `nav_t1`、`btn_refresh`），**不要**用中文、空格、连字符作 id；**中文与标点只出现在**双引号内的**显示文本**中。
  6. 若**仍不渲染**，优先检查：引号成对、围栏三反引号闭合、**去掉**不支持的 `&` 多路后重试；必要时略缩短标签长度或**拆成**两张更小的 `flowchart` 以降低语法复杂度。

  最小可渲染示例（可整段复制进 PRD；也支持用三个反引号作围栏，与 `mermaid` 同效）：

  ~~~mermaid
  flowchart TB
      subgraph s_nav["顶部导航"]
          t1["首页"]
      end
      t1 --> n_done["他页-待确认路由"]
  ~~~
- **语言**：默认 **简体中文**，术语与界面文案保持一致。
- **受众表述**：生成的 PRD 尽量使用**开发、测试熟悉**的写法——例如前置/后置条件、数据与界面状态、异常与边界、权限与错误码（若已知）、可对照验收的条目化描述；少用空泛形容词或仅业务口号式句子，便于实现评审与用例设计。
- **与 2.4 一致**：完整 PRD 须在 **2.4 全局交互与展示约定**中固定默认条款（防连点+loading、列表默认按操作时间倒序、列表/表单/图表可感知加载须给 loading、文字超长省略+悬停气泡全文、**进行中状态自动更新无需手动刷页**）；详文见 `references/global-ui-conventions.md`。单个功能小节可写「同 2.4」或只写差异项。
- **按钮防连点与 loading（详述须可验收）**：凡会触发接口请求、写操作或带副作用的交互控件，须写明：请求中 **loading**、**禁用该按钮**（或等价禁用整表单/整弹窗主操作，二选一写清）；结束后恢复。纯前端无请求交互可不写；与请求绑定则适用。
- **交付前必做三角色自查（自动，非可选）**：每轮生成 PRD 后**自动**按 [`references/triple-role-self-review.md`](references/triple-role-self-review.md) 执行，**勿**等用户口头触发；修订合并进 PRD 正文，自查过程不写入 PRD。用户显式要求跳过时可省略。
- **原型图嵌入（页面类，默认必做）**：凡使用列表/卡片/表单/看板/信息展示等**含「1 原型截图」**的模板，交付稿中该节须出现 **至少一张** Markdown 图片引用：`![简要图注](相对路径)`（**推荐** `assets/prototypes/文件名.png`，与 PRD 同级的 `assets/` 目录，**可省略** `./` 前缀）；同一功能多屏、多状态（默认态、筛选结果、空状态、错误态、局部迭代圈选等）可**多张图上下排列**，每张配简短图注或小标题。**路径**以 PRD `.md` 文件所在目录为根（预览器按**该 md 文件路径**解析，不是按聊天窗口或任意粘贴位置）。**仅有对话附件、工作区尚无对应文件时**：仍写出**目标相对路径与文件名**，并加一句业务可读提示：「请将本轮原型/设计截图保存至上述路径」；若环境允许写入且用户未反对，可将附件**保存到该路径**再引用。**禁止**只保留模板括号里的「粘贴列表页原型…」类说明而**无任何** `![]()`。**纯 AI/策略类**无界面可不嵌图；若有简单配置页/结果页截图，同样在详述靠前位置嵌入。
- **原型图为何「不显示」**（排查）：① 是否在 **Cursor/VS Code 中打开该 `.md` 文件** 并使用 **Markdown 预览**？聊天里粘贴的 Markdown **通常无法加载**工作区相对路径图片。② 是否将 PRD **单独拷走**而未带上 **`assets/prototypes/`** 下文件？③ **alt 文案**避免使用易与 Markdown 冲突的写法（尽量少用全角括号 `（）` 嵌在 alt 内，可改用冒号或英文括号）。④ 仍不显示时安装/启用带 **本地资源** 支持的 Markdown+Mermaid 预览扩展。⑤ **Mermaid 图不显示、只显示代码**：见上文「**为 PRD 可渲染、少踩坑的 Mermaid 写法**」各条（尤其是**双引号**文案、**不要**用 `&` 多路连写、**三反引号围栏**成对、节点 **id** 用英文+下划线）。

---

## 使用示例

**示例 1：单页列表需求（默认只出详述）**  
用户：[上传列表页原型]「这是数据集任务列表改版，写需求详述」  
智能体：按列表页模板结构输出 `### 3.x …` 功能小节（含筛选、批量、行操作、分页等），**「1 原型截图」** 含 `![](./assets/prototypes/…)` 等；**文内不写「第3章」「按模板」**。

**示例 1b：口语「写 PRD」仍只出第三章**  
用户：[上传截图]「请生成这个功能的 PRD」  
智能体：视为**未指定全章**，只输出 **`## 3 产品需求详述`** 下与本图对应的一个 **`### 3.x …`** 小节（按模板），**不**写 **1 概述、2 全局说明、4～9**；若需全文档，可一句反问是否要「完整 PRD（1～9 章）」。

**示例 2：表单新建页**  
用户：[上传表单原型]「按表单模板写 PRD」  
智能体：按表单页结构补齐字段表与校验；**交付稿不出现模板文件名**。

**示例 3：局部迭代**  
用户：[上传圈选「导出按钮」位置的原型]「只在列表页加导出」  
智能体：仍按列表页结构，在文首或功能说明中标明局部迭代；详述仅写导出及相关权限、异步、限流与提示。

**示例 4：AI 策略**  
用户：「没有页面，大模型根据工单摘要生成回访话术」  
智能体：按 AI/策略类结构写输入输出、Good Case、Bad Case 与降级。

**示例 5：指定多章（顶级）**  
用户：「输出章节 1、2、3、4」  
智能体：依次撰写 **1 概述、2 全局说明、3 产品需求详述**（需材料齐备）、**4 埋点统计**；正文从标准标题起笔，**不交代「共几章」**。

**示例 6：完整 PRD**  
用户：「各功能详述已贴在文档里，生成完整 PRD」  
智能体：按 `full-prd-skeleton.md` 排序汇编已有详述，并补全 **1、2、4～9** 等缺章；**不向读者解释目录文件名或章节总数习惯**。

**示例 7：信息不足 · 暂停生成**  
用户：「帮我写个 PRD」[无附件、无背景]  
智能体：**不输出** PRD 正文；说明缺少原型/类型/范围等；列出若干具体问题（是否页面类、是否可补截图、目标用户等），请用户补充后再生成。

**示例 8：三角色自查为每轮内置步骤（无需用户说「请自查」）**  
用户：[已满足材料条件]「按原型写详述」  
智能体：先按模板输出 **3** 下功能小节；**随即自动**打开 `references/triple-role-self-review.md` 完成三角色自查，**直接合并修订**到同一 Markdown（补主路径与异常、与 1.2/2.3 对齐、逆向操作与权限等）；**不**在 PRD 内粘贴自查表；**不**等用户再发一条「复核一下」。若用户只要终稿、不要说明，可只回复**已自查修订后**的 PRD。